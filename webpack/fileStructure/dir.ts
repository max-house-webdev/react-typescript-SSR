const path = require('path');

const dir = {
  root: path.resolve(__dirname, '../../'),
  src: '/src',
  core: '/core',
  dist: '/dist',
  server: '/server',
  client: '/client',
  static: '/static',
};

export default dir;
